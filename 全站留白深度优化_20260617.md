# 恒生石材网站 - 全站留白深度优化

## 完成时间
2026-06-17 09:06 HKT

## 执行结果

### CSS 变量与全局间距收紧 ✅
| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| `--section-padding` | 60px | **40px** |
| `--gutter` | 48px | **40px** |
| 移动端 section-padding | 32px | **24px** |
| 1920px+ 断点 | 100px | **56px** |
| 1440-1919px 断点 | 90px | **52px** |
| 1200-1439px 断点 | 80px | **48px** |
| Footer padding | 48px 0 0 | **32px 0 0** |
| Footer-grid gap | 28px | **20px** |

### CSS Gap 全面收紧 ✅
| 选择器 | 修改前 | 修改后 |
|--------|--------|--------|
| `.about-grid` | 80px | **32px** (大屏 56px/52px) |
| `.contact-grid` | 60px | **36px** |
| `.product-cat-grid` | 60px | **36px** |
| `.advantages-grid` | 32px | **24px** |
| `.partners-strip` | 48px | **32px** |
| `.stone-color-groups` | 48px | **32px** |
| `.product-detail-hero` | 48px | **32px** |
| `.why-choose-grid` | 48px | **32px** |
| `.contact-philosophy-grid` | 32px | **24px** |
| `.info-cards/.service-cards/.advantage-cards/.compare-cards` | 16px | **12px** |
| `.stats-row` margin-top | 32px | **20px** |

### HTML Inline Padding 移除 ✅
- **81 个 HTML 文件中移除了 231 处 inline padding 覆盖**
- `padding:60px 0` → 全部移除（让 `.section-padding` CSS 类统一控制）
- `padding:48px 0` → 全部移除
- `padding:60px 0 60px` → 全部移除
- `margin-bottom:60px` → 24px
- `padding:160px 0` → 80px 0（仅 hero 区域）
- 96 个 section 添加了 `section-padding` class（确保移除 inline padding 后仍有正确的间距）
- 22 个 `products-cta` section 添加了 `section-padding` class

### 验证结果 ✅
- CSS 大括号平衡：1713 对 1713 ✅
- 残留 inline `padding:60px 0`：0 ✅
- 残留 inline `padding:48px 0`：0 ✅
- 孤立 CSS 值：0 ✅

### Git
- Commit: `1a29d83` on `main`
- Message: `feat: 全站留白深度优化 - section-padding 60→40px, 移除231处inline padding覆盖, gap/gutter全面收紧`
- 78 files changed, 488 insertions(+), 488 deletions(-)
- Pushed to remote ✅

### 修复的附带问题
1. `en/sample-request.html` 的 `.sample-form-section` 原始 CSS 有 bug（缺少 `padding:` 前缀），已修复
2. `sample-request.html` 的 `.sample-hero` padding 从 80px 0 36px 缩减至 48px 0 24px
3. 组件级 padding 也做了缩减：`.role-entry-section`、`.timeline-section`、`.tech-section`、`.sustainability-section` 从 64px → 40px

### 未修改的项目（正确保留）
- 导航侧边栏 padding（mobile nav-links）：这些不是 section 间距，保持不变
- Cookie banner 的 padding：内部组件间距，保持不变
- 面包屑区域的 `padding:24px 0`：适当的小间距，保持不变
- 所有 background、color、border 等非间距样式：未改动
